gl_vis_temp['jbus_interface'] = {
	init: function($this) {
		$this.append('<img style="display:inline;" src="img/jbus_intf.png" />')
		
		$this.data('node_num',0);
		
		$this.click(
				function() {
				});
	},
	vis: function($this, vis) {
	},
	edit: function($this, vis) {
	}
}